#include <iostream>

using namespace std;

int main()
{
    string arr;
    char stack[100];
    int top = -1,l = arr.length();
    for(int i=0;i<l;i++){
        if(arr[i] == '(')
        {
            stack[++top] = arr[i];
        }
        else{
            
        }
    }
}